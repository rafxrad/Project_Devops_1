name: CICD

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: checkout source
        uses: actions/checkout@v3
      - name: Login to Docker Hub
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - name: enable runner
        run: |
          cd /home/ubuntu/actions-runner
          ./run.sh
      - name: move to path
        run: |
          cd /home/ubuntu/actions-runner/_work/Project_Devops_1/Project_Devops_1
      
  deploy:
    needs: build
    runs-on: aws-ec2
      - name: Pull back image from docker hub
        run: docker pull eversonnascimento/back-todo:v1.0
      - name: Pull front image from docker hub
        run: docker pull limanascimento/supertodolist:v1.1
      - name: Check directory
        run: |
            pwd
            ls -la
      - name: Install Docker Compose
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose
      - name: Run docker compose
        run: docker-compose up -d


## commit again